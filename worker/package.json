{
  "name": "rosalie-checkout-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node src/promotion.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
